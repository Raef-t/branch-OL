import 'package:flutter/material.dart';

class CustomPreviouslySearchInSearchView extends StatelessWidget {
  const CustomPreviouslySearchInSearchView({
    super.key,
    required this.text,
    required this.index,
  });
  final String text;
  final int index;
  @override
  Widget build(BuildContext context) {
    return const Column(children: []);
  }
}
